# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=flashmovies_db
DB_USER=flashmovies_user
DB_PASSWORD=changeme

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# PayPal Business Payments
# Get these from https://developer.paypal.com/dashboard/applications
STRIPE_SECRET_KEY=sk_test_YOUR_STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET
# Set to 'sandbox' for testing or 'live' for production

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:5173

# Local dev seed (optional overrides for npm run seed:dev)
# SEED_USER_EMAIL=test@flashmovies.local
# SEED_USER_PASSWORD=password123
# SEED_PRO_USER_EMAIL=pro@flashmovies.local
# SEED_PRO_USER_PASSWORD=password123
